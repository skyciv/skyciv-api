### v2.2.5 - 09 March 2026
* Added ability for renderer to handle dimension lines.

### v2.3.0 - 21 Aug 2026
* Added pen tool (can be used by passing in `pen_tool:true` when initialising the renderer).
* Added `CTRL + Left-click drag` action for mouse select boxes.
* Modernised the bottom menu styling.
* Mouse will now zoom at location of mouse rather than center of screen.
* Changed global axis in bottom-left to match the main renderer in S3D (might have been slightly before v2.3.0).
* Added line supports (might have been slightly before v2.3.0).
* Other fixes.